<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron: Synchronize all active Dotykačka configurations -->
        <record id="cron_dotykacka_sync_all" model="ir.cron">
            <field name="name">Dotykačka: Sync All Configurations</field>
            <field name="model_id" ref="model_dotykacka_config"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_all_configs()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>

        <!-- Cron: Clean up old sync logs -->
        <record id="cron_dotykacka_cleanup_logs" model="ir.cron">
            <field name="name">Dotykačka: Cleanup Old Logs</field>
            <field name="model_id" ref="model_dotykacka_sync_log"/>
            <field name="state">code</field>
            <field name="code">model.cleanup_old_logs(days=30)</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>

    </data>
</odoo>
